# Copy to .env (or just export these). Everything works without them.
ORS_API_KEY=            # OpenRouteService - real road-following routes on the map
GEMINI_API_KEY=         # Policy bot phrasing; retrieval-only without it
